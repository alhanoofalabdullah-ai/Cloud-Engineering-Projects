# Environment Variables

Environment variables are used to store configuration outside the application code.

## Examples

```text
APP_ENV=production
APP_NAME=railway-java-app
APP_VERSION=1.0.0
LOG_LEVEL=INFO

---

Best Practices

* Do not hardcode secrets in source code
* Use environment variables for configuration
* Separate development and production values
* Keep sensitive values private
